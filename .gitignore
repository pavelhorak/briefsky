# Logs
logs
*.log
npm-debug.log*
node_modules
misc
*.local
.DS_Store
test-results/

# Build output. Regenerated by `npm run deploy`, and never reproducible: vite.config.ts
# stamps a timestamp-based build id into the bundle (and version.json) so the in-app
# auto-updater can detect new deploys, so every build differs by design.
dist/
